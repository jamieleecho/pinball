name: Bump and Tag Version

on:
  workflow_dispatch:

jobs:
  bump:
    if: "!contains(github.event.head_commit.author.name, 'version-bumper')"
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      # package.json is the only place the version lives: the Xcode project
      # reads it when scripts/gen-xcode.py regenerates the project, so there is
      # no plist line number to keep in step.
      - name: Bump Versions
        uses: michmich112/version-bumper@master
        env:
          CI: false
        with:
          options-file: './bump-info.json'
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Trigger release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh workflow run make-release.yml --ref `grep "version" package.json | sed 's/.*: *"//' | sed 's/" *//'`
